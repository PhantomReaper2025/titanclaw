# Workspace Memory

Long-lived operational memory for TitanClaw.

## Stable Facts

- Product name is `TitanClaw`.
- Primary repository is `https://github.com/PhantomReaper2025/titanclaw`.
- Default user goal is a production-ready secure AI runtime with strong sandbox workflows.

## Operational Decisions

- Keep onboarding simple with strong defaults for OpenRouter and sandbox runtime selection.
- Prefer automatic recovery paths (image preflight, fallback build) over manual troubleshooting.
- Keep documentation synchronized with real implementation status.

## Follow-up Rules

- Promote important temporary notes into this file.
- Delete outdated entries quickly.
- Mirror user-facing behavior changes in `README.md` and `CHANGELOG.md`.
